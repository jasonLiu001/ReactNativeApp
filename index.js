import {AppRegistry} from 'react-native';
// import App from './app.stack.navigation';
import App from './app.tab.navigation';

// AppRegistry.registerComponent('ReactNativeApp', () => App);
AppRegistry.registerComponent('ReactNativeApp', () => App);